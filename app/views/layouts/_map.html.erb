<div class="map wow fadeInUp" data-wow-delay="0.4s">
    <div id="map" style='width: 500; height: 500;'></div>
  </div>
<%= submit_tag "Zoek naar ingredienten in de buurt", :name => nil %>
<%= form_tag ingredients_path, :method => :get do %>
  <%= text_field_tag :search, params[:ingredient] %>
 <% end %>

 <button><%= link_to 'Ingredient toevoegen', new_ingredient_path %></button>
  <div class="row">
    <div class="t column">
  
</div>
</div>
  
 
<!--  -->
  <script type="text/javascript">
  handler = Gmaps.build('Google');
  handler.buildMap
  ({provider: {}, internal: {id: 'map'}}, function()
  { markers = handler.addMarkers(<%=raw @hash.to_json %>);
  handler.bounds.extendWith(markers);
  handler.fitMapToBounds();
  DirectionsService();
  });
</script>